# خطة المشروع — Technosham

> هذه الخطة قابلة للنقاش والتعديل. أعطني ملاحظاتك وسنحدّثها معاً.

---

## ✅ المرحلة 0 — التهيئة (مكتملة)

- [x] حذف الملفات القديمة من المستودع.
- [x] إنشاء `info.md` و `plan.md`.

---

## 🚧 المرحلة 1 — الموقع الثابت (HTML/CSS/JS)

الهدف: معاينة التصميم والألوان بسرعة ونشره على GitHub Pages.

### البنية
```
technosham/
├── index.html          ← الصفحة الرئيسية
├── css/
│   └── style.css       ← كل التنسيقات
├── js/
│   └── main.js         ← التفاعلات (قائمة الجوال، الأنيميشن، إلخ)
├── assets/             ← الصور والأيقونات (لاحقاً)
├── info.md
├── plan.md
└── README.md
```

### الأقسام المطلوبة
- [x] واجهة Hero بجملة جذّابة وزر CTA.
- [x] قسم "من أنا".
- [x] قسم "الخدمات" (بطاقات).
- [x] قسم "المشاريع" (معرض بطاقات).
- [x] قسم "آراء العملاء" (Testimonials).
- [x] قسم "التواصل".
- [x] شريط تنقّل علوي + قائمة للجوال.
- [x] فوتر.

### المميزات التصميمية
- [x] تصميم داكن + نيون متوهّج.
- [x] خلفية متدرّجة متحرّكة.
- [x] تأثيرات Glassmorphism للبطاقات.
- [x] أنيميشن ظهور عند التمرير (Scroll Reveal).
- [x] تأثير كتابة (Typing) في الواجهة.
- [x] تصميم متجاوب بالكامل (جوال/تابلت/كمبيوتر).

### النشر
- [ ] رفع الكود إلى فرع `claude/portfolio-website-setup-44g7h2`.
- [ ] إنشاء Pull Request.
- [ ] تفعيل GitHub Pages من إعدادات المستودع (المستخدم يقوم بذلك).

---

## ✅ المرحلة 2 — Django + Next.js + لوحة التحكم (مكتملة)

الهدف: جعل الموقع ديناميكياً وقابلاً للإدارة عبر لوحة تحكم، مع API مفتوح.

### الخلفية (Django + DRF) — `backend/`
- [x] مشروع Django 5 + Django REST Framework + JWT + CORS.
- [x] تطبيق `content`: SiteSettings (المظهر)، Service، Project، Testimonial، ProcessStep، Stat، ContactMessage.
- [x] تطبيق `integrations`: ExternalSite + MetricSnapshot + **API عام (/api/ingest/)** لاستقبال إحصائيات مواقعك.
- [x] لوحة Django Admin لكل النماذج.
- [x] نقطة `/api/bootstrap/` تُرجع كل محتوى الموقع في طلب واحد.
- [x] نقطة `/api/overview/` تجمع إحصائيات كل المواقع المرتبطة.
- [x] أمر `seed` لتعبئة بيانات تجريبية + مستخدم مشرف.

### الواجهة (Next.js 14 App Router) — `frontend/`
- [x] الموقع العام يُبنى من الـ API (المظهر + المحتوى) مع بيانات افتراضية احتياطية.
- [x] نظام مظهر عبر متغيّرات CSS (ألوان/خط/حجم) تُحقن من الإعدادات.
- [x] **لوحة تحكم** كاملة: تسجيل دخول (JWT)، نظرة عامة، إدارة المشاريع، المواقع المرتبطة، الرسائل.
- [x] زر **«المظهر»** يفتح سايدبار لتغيير الألوان/الخط/الحجم/الاستدارة/الوضع الداكن مع **معاينة حيّة** وحفظ.
- [x] كل الحركات محفوظة (النموذج الطافي، العمق مع الماوس، النص المتدرّج).

### النشر (المرحلة 2)
- [ ] نشر Next.js على Vercel (مجاني).
- [ ] نشر Django + قاعدة البيانات على Render / Railway (مجاني).
- [ ] ضبط `NEXT_PUBLIC_API_URL` و `CORS_ALLOWED_ORIGINS` في الإنتاج.

> **ملاحظة:** الموقع الثابت القديم (`index.html` في الجذر) لا يزال موجوداً ويعمل على GitHub Pages حتى تُقرّر استضافة النسخة الجديدة.

---

## 📌 نقاط للنقاش معك

1. ~~قسم "آراء العملاء"~~ ✅ تمت إضافته (ببيانات وهمية حالياً).
2. هل تريد صفحة مدونة (Blog)؟
3. ما المشاريع الحقيقية التي نعرضها؟ (الأسماء + الروابط + الصور)
4. ما بيانات التواصل الفعلية؟ (حالياً كلها قيم وهمية يجب استبدالها).
5. هل تريد وضع "الوضع الفاتح/الداكن" (Light/Dark toggle)؟

> **ملاحظة:** جميع البيانات حالياً وهمية (الاسم: تكنو شام، الأرقام، الروابط، آراء العملاء) — يجب استبدالها بالبيانات الحقيقية لاحقاً.
